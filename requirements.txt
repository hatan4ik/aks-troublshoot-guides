# ── Core — required for all CLI commands ─────────────────────────────────────
kubernetes==28.1.0
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
aiofiles==23.2.1
prometheus-client==0.19.0
requests==2.31.0

# ── TLS certificate expiry check ─────────────────────────────────────────────
# Install with: pip install -r requirements-tls.txt
# cryptography>=41.0.0

# ── AKS (Azure) provider checks ──────────────────────────────────────────────
# Install with: pip install -r requirements-azure.txt
#   azure-identity>=1.15.0
#   azure-mgmt-network>=25.0.0
#   azure-mgmt-compute>=30.0.0
#   azure-mgmt-containerservice>=28.0.0
#   azure-mgmt-authorization>=4.0.0
#   azure-mgmt-privatedns>=1.0.0

# ── EKS (AWS) provider checks ────────────────────────────────────────────────
# Install with: pip install -r requirements-aws.txt
#   boto3>=1.34.0

# ── GKE (GCP) provider checks ────────────────────────────────────────────────
# Install with: pip install -r requirements-gcp.txt
#   google-cloud-container>=2.40.0
#   google-auth>=2.26.0
#   google-api-python-client>=2.115.0

# ── AI/ML predictor helpers (experimental) ───────────────────────────────────
# Install with: pip install -r requirements-ai.txt
#   numpy>=1.26.0
#   scikit-learn>=1.4.0
